# Chat

# Functionality

- [x] User is able to send messages to the chatbot
- [x] User is able to receive messages from the chatbot
- [x] Chatbot remembers the conversation upon reload
- [x] Chat history is stored in localStorage

# HTML

- [x] HTML is well structured can contains the main components required by the app, including a header, main chat area, an input field, a send button, and a new chat button
- [x] HTML uses proper semantic tags rather than just <div> and <span> everywhere
- [x] HTML uses meaningfully named IDs and classes for styling and DOM manipulation

# CSS

- [x] CSS makes use of at least one flexbox, if not more to properly layout elements
- [x] CSS makes good use of colors, either built-in or though hex or RGB codes
- [x] App makes good use of the box model with proper margins, padding, and borders where applicable

# UI/UX

- [x] Send button is visible and clickable
- [x] Input field is visible and clickable
- [x] Send button is disabled when the input field is empty
- [x] Chat messages are displayed in a scrollable container, with consideration to both large and small screen sizes
- [x] Sender and receiver are displayed in a different color

# Code

- [x] script.ts is written in TypeScript, and compiles to script.js within the dist folder
- [x] Code has no TypeScript errors, warnings, or bugs
- [x] No 'any' types are parsed in TypeScript (unless a good reason is provided)

Note: We will only style check the .ts file!